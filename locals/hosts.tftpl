all:
  hosts:
    router-1:
      ansible_host: ${ne_ip}
      # ansible_password: dtairych
      # ansible_user: newpassword
      # ansible_become_password: newpassword

routers:
  hosts:
    router-1
  # vars:    
  #   ansible_connection: ansible.netcommon.network_cli
  #   ansible_network_os: cisco.ios.ios
  #   ansible_become: yes
  #   ansible_become_method: enable
